<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- browserConfig theme -->
    <!-- standard, dark -->
    <theme>Standard</theme>

    <!-- document encoding -->
    <!-- defaults to UTF-8 -->
    <encoding>UTF-8</encoding>

    <!-- protocol for script and stylesheets -->
    <!-- defaults to https -->
    <protocol>HTTPS</protocol>

	<!-- offline report -->
	<timelineEnabled>true</timelineEnabled>

	<!-- offline report -->
	<enableOfflineMode>false</enableOfflineMode>

    <!-- title of the document -->
    <documentTitle>Skyra Automation Report</documentTitle>

    <!-- browserConfig name - displayed at top-nav -->

    <reportName> <![CDATA[ <img src='../Resources/logo.png' />  ]]> </reportName>

    <!-- location of charts in the scripts view -->
    <!-- top, bottom -->
    <testViewChartLocation>top</testViewChartLocation>
    <enableCategoryView>true</enableCategoryView>
    <enableAuthorView>true</enableAuthorView>
    <enableExceptionView>true</enableExceptionView>
    <enableTestRunnerLogsView>true</enableTestRunnerLogsView>

    <!-- custom javascript -->
    <scripts>
        <![CDATA[
                $(document).ready(function() {

                });
            ]]>
    </scripts>

    <!-- custom styles -->
    <styles>
        <![CDATA[
               .report-name { padding-left:85px; }
               .report-name > img { float:left; width:auto; position:fixed; }
			   .badge-primary { background-color: white; color: black }
			   .header-dark .header { background-color: white; }
			   .opacity-01 { opacity: 100; }
			   
            ]]>
    </styles>
</configuration>
